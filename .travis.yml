before_install:
  - npm install -g bower
  - npm install -g grunt-cli

before_script:
  - bower install

language: node_js
node_js:
  - "0.10"
  - "0.8"
notifications:
  irc:
    channels:
      - "chat.freenode.net#hoodie"
    use_notice: true
    skip_join: true
